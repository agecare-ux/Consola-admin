# Versiones fijadas a las verificadas el 14-09-2026 (mismas que instala el entorno
# local y las que construye Vercel). Sin fijar, dos despliegues del mismo código
# pueden traer versiones distintas y fallar sin que nadie haya tocado nada.
# Para actualizar: subir la versión aquí, correr pytest y las dos auditorías.
fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
alembic==1.19.1
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0
argon2-cffi==25.1.0
PyJWT==2.13.0
pyotp==2.10.0
jsonschema==4.26.0
